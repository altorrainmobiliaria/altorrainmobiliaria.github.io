# Dependencias Node.js
node_modules/
.npm/

# Backups de Firestore (pueden contener datos sensibles)
backups/

# Credenciales Firebase (NUNCA commitear)
*.json.key
serviceAccount.json
service-account.json
sa.json
firebase-debug.log
firebase-debug.*.log

# Variables de entorno
.env
.env.local
.env.production

# macOS
.DS_Store
.AppleDouble
.LSOverride
__MACOSX/

# Editor
.vscode/settings.json
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Build output temporal
.cache/
dist/

# Material estrategico interno del dueno (respaldado en brain-private/pauta) - NO al repo publico
Brief_Dise*.docx

# Workspace de Antigravity (copia canonica de sus skills adoptadas vive en skills/)
.agents/
_tmp-guia-*.pdf

# Paquete de contexto de las auditorias del kit: contiene COPIAS INTEGRAS de los
# contratos 03/04 y del contexto legal. Este repo es PUBLICO (GitHub Pages) -> jamas al repo.
# Su casa definitiva es la boveda privada brain-private/altorrainmobiliaria/.
.auditoria-contratos/

# Cache de tipos generada por Astro (portal)
.astro/

# TODO-28 #1: foto de sesion del hook (estado local, no historia)
docs/.handoff-auto.md
docs/.boot-marker
docs/.estado-auto.md
docs/.consolidacion-pendiente
# Alerta del VIGIA de la boveda (PLAN-CIERRE §5): la levanta scripts/vigia.mjs de
# brain-private cuando algo del cerebro sale rojo sin nadie delante, la COBRA el
# SessionStart y la borra el pre-commit cuando el vigia vuelve a dar verde. Estado
# local de ESTA maquina, igual que el flag de consolidacion: no es historia.
docs/.vigia-alerta
.playwright-mcp/

# Rastros de la pieza B (falencias del modelo, 2026-09-03). Efimeros y por-maquina:
#  .guard-log    lo escribe el hook PreToolUse scripts/guard-destructivo.mjs (B2) con cada
#                decision deny/ask — su CONTADOR es la medicion del mecanismo.
#  .promesas-log lo escribe session-handoff --end (B7) cuando un turno cierra prometiendo el
#                siguiente paso en vez de ejecutarlo. Se mide 30 dias antes de decidir si se queda.
docs/.guard-log
docs/.promesas-log

# Carpetas que escriben plugins de terceros dentro del repo (incidente 2026-09-03).
# No son nuestras y una de ellas llevaba un id de telemetria dentro.
.semgrep/
.remember/
