# 🗺️ 20 — MEMORIA ESPACIAL (Altorra Inmobiliaria · arquitectura/flujos verificados)

> Trigger de Desorientación (§G.2): dónde vive cada cosa, schema Firestore, free-tier, flujos.
> ⚠️ Schema/módulos destilados de `_legacy/CLAUDE.md` (constitución) — verificar contra el repo real antes de codear (§3.3).

---

## §Stack y módulos JS
- **Frontend**: vanilla + Firebase **modular v12.9.0 ESM** (CDN gstatic). NO namespacing admin/public (a diferencia de cars). **Node/scripts/CFs**: `firebase-admin v13`. (Doctrina de cómo escribir código → `34-DOCTRINA-CODIGO`.)
- **Globals `window.*`**: `db`, `auth`, `storage`, `functions`, `firebaseAnalytics`, `rtdb`, `propertyDB` (clase `PropertyDatabase`), `AltorraCache`, `AltorraUtils`, `AltorraFavoritos`. Readiness: evento `altorra:db-ready` (`await waitForDB()`).
- **Archivos JS** (kebab-case en `js/`) — inventario **RE-CONTADO contra el disco** el 2026-08-02: **36 archivos**. Antes se listaban 12 y **dos no existían**: `render.js`/`renderPropertyCard()` (no existe esa función en NINGUNA parte del repo) y `toast.js` (el toast vive en `utils.js`). Verificable: `node` + `readdirSync('js')`.
  - **Núcleo**: `firebase-config.js` · `database.js` (`PropertyDatabase`) · `cache-manager.js` (en `js/`, **NO** `js/core/`) · `utils.js` (`AltorraUtils`, **aquí vive `showToast`**) · `components.js` (inyecta header/footer/modals, `loadModalsIfNeeded()`) · `i18n.js` · `firestore-meter.js` (medidor de lecturas, guardián del free-tier).
  - **Catálogo y listado**: `listado-propiedades.js` · `sector-properties.js` · `smart-search.js` (fuzzy Damerau-Levenshtein) · `comparador.js` · `exclusivas.js` · `mapa-propiedades.js` · `featured-week-banner.js` · `investment-badges.js`.
  - **Conversión y leads**: `contact-forms.js` · `wizard-publicar.js` · `wizard-visita.js` · `newsletter.js` · `exit-intent.js` · `whatsapp-tracker.js` · `country-phone.js`.
  - **Herramientas**: `simulador-hipotecario.js` · `calculadora-airbnb.js`.
  - **Favoritos e historial**: `favoritos.js` · `favorites-manager.js` · `historial-visitas.js`.
  - **Blog**: `blog-list.js` · `blog-post.js`. · **Analítica**: `analytics.js`.
  - **Admin**: `admin-auth.js` · `admin-dashboard.js` · `admin-kanban.js` · `admin-leads.js` · `admin-properties.js` · `admin-users.js`.
- **Cloud Functions (Node 20, `us-central1`) — 9 en CÓDIGO · 7 DESPLEGADAS.** Censo re-hecho el 2026-08-02 contra `functions/index.js` **y contra producción** (`verificado-vivo: 2026-08-02`, MCP Firebase). La discrepancia vieja (`05` decía 7 · `20` decía 8 y nombraba `cleanupOldLoginAttempts`, que **no existe en ninguna parte**) no era descuido: **cada nodo contaba una cosa distinta y el cerebro no tenía esa distinción**. Ahora se declara explícita.
  - **DESPLEGADAS (7)**: `onNewSolicitud` (email admin + lead scoring) · `onSolicitudStatusChanged` (email cliente) · `onPropertyChange` (regen SEO, debounce 5 min) · `triggerSeoRegeneration` (callable super_admin) · `createManagedUserV2` / `deleteManagedUserV2` / `updateUserRoleV2`.
  - **EN CÓDIGO, NO desplegadas (2)** — son las únicas que actúan hacia AFUERA por sí solas, y por eso importan: **`processNurturingEmails`** (`onSchedule` cada 6 h; consulta `solicitudes` con `nurturing.nextEmailAt` vencido, `limit(50)`, y envía seguimiento a leads con los secrets `EMAIL_USER`/`EMAIL_PASS`) y **`sendNewsletter`** (callable super_admin → TODOS los suscriptores activos). **Desplegarlas es una DECISIÓN de negocio, no fontanería**: la primera empieza a escribir a personas reales sin que nadie la dispare. No tocar en el cutover sin decidirlo aparte.
- **Admin SPA** = `admin.html`, objeto global `window.IP`. RBAC 3 roles, lockout 5 intentos, sesión 8h, inactividad 30min.

## §Portal (greenfield · Astro 7 + CF Workers) — dónde vive cada cosa
> ⚠️ El portal (`portal/`) es SEPARADO del sitio legacy de §Stack. Stack sellado ADR §16.
- **Diseño fuente (SSoT visual)**: `portal/design/` — `mockups/*.dc.html` (**9** aprobados por Daniel), `assets/` (logo+fotos), `VISION-DUENO.md` (paleta+visión funcional), `screenshots/`.
- **Design system D1** (ADR §23 + §23.8): `portal/src/styles/tokens.css` (SSoT `--alt-*`; §1 = **paleta OFICIAL** navy #062743/dorado #D4AF37/plateado #BFC3C9/grises fríos; §14 = **Liquid Glass** `--alt-glass-*`) → `base.css` (reset+tipografía+a11y) → `components.css` (primitivas `.alt-btn/.alt-pill/.alt-card/.alt-eyebrow/.alt-chip/.alt-badge/.alt-seal/.alt-field/.alt-alert` + **`.alt-glass`/`.alt-glass--dark`/`.alt-nav-glass`** + firma única **`.alt-card--vitrina`** neu+glass+filo oro; de la saga §32: `.alt-rail`/`.alt-rnav` rieles compartidos + `.alt-btn-sweep`/`.alt-btn-frost` + `.home-band--neu`). Importados EN ORDEN en `src/layouts/BaseLayout.astro` (+ fuentes Google Cormorant Garamond + Hanken Grotesk). **Styleguide viva** = `/design-system` (dev/noindex — excluir de prod en cutover). **Lenguaje**: NEUMORFISMO protagonista + Liquid Glass SUTIL. **Superficie DUAL-MODE**: `--alt-surface #fff` default (plano) · `--alt-surface-neu #eaf0f7` (home+nav neumórfico) · `--alt-surface-ink #062743` (secciones). A11y AA verificado (WCAG vs #eaf0f7).
- **Capa de datos** (ADR §22): `src/lib/data/` (`client.ts`+`firestore-rest.ts`+`cache.ts`, edge-safe REST) · `src/lib/domain/` (tipos) · `src/middleware.ts` (cablea `locals.altorra`). Firebase emulador+seed en `portal/firebase/`.
- **Catálogo denormalizado** (ADR §54 decisión + §56 obra-lectura): colección `indices/catalogo-{venta|arriendo|dias}` (resúmenes lean de PUBLICADAS, solo la Function escribe). LECTURA: `src/lib/domain/catalogo.ts` (tipos) · `client.ts` repo `catalogo.get(shard)` · rules `indices/{doc}` (allow-list) · ruta `src/pages/api/catalogo/[operacion].json.ts` (edge-cache purgeable). ESCRITURA: NÚCLEO en `domain/catalogo.ts` (`construirIndices`, PURO — §57) + PLOMERÍA en **`portal/functions/`** = codebase `portal` AISLADO del legacy (§58; declarado en `portal/firebase/firebase.json`, deploy `--only functions:portal` en el CUTOVER): `src/catalogo-rebuild.ts` (query+transacción+guarda anti-adelantamiento L-35) y `src/index.ts` (triggers onWrite/barrido-5min/callable Republicar). Control privado en `indices/_control`. Tests emulador → `firebase/tests/catalogo-rebuild.test.ts` (`npm run test:rules`). CONSUMO: isla `src/scripts/serp-catalogo.ts` en el SERP tras `PUBLIC_CATALOGO_SOURCE=demo|live` (§59; default demo = no-op) — clona el `<template id="tpl-pcard">` de `PropertyCard` y actualiza los pines con `setMarkers()` de `altorra-map`. Ruta DORMIDA hasta cutover (SERP sigue demo).
- **Mapa real** (ADR §55, TODO-30): isla client-side `src/scripts/altorra-map.ts`(+`.css`) = MapLibre GL v6 + Protomaps basemaps v5 (pines-precio navy/oro + card↔pin por `data-pin-idx` + fallback al esquemático). Basemap `.pmtiles` servido desde **R2** (binding `R2_MEDIA`) por la ruta con RANGE `src/pages/tiles/[file].ts`. ⚠️ Bindings CF en rutas SSR = `import { env } from 'cloudflare:workers'` (NO `locals.runtime.env`, removido en Astro v6 — L-33). Generar/subir el `.pmtiles` → `50 §Tiles`.
- **Componentes** (`src/components/`, ADR §24): **`Header.astro`** (nav sticky COMPARTIDA 3 capas: util+glass+drawer; data-driven `nav[]`; dropdowns CSS; JS drawer+scroll; prop `active`; contacto REAL) · **`Footer.astro`** (navy COMPARTIDO 5 columnas + barra legal; razón social/NIT/contacto REALES; matrícula `000000` placeholder) · **`PropertyCard.astro`** (card de inmueble REUTILIZABLE: imagen+fav+badge+specs+título Cormorant+precio+orbe; props tipados) · **`LuCard`/`StayCard`/`RankCard`/`ProjectCard`** (§32.11-.13: **5 cards en total, NINGUNA intercambiable** — no derivar unas de otras). Assets logo+fotos en `public/assets/` (JPG sin optimizar → TODO WebP <150KB).
- **CENSO de páginas — este nodo es el DUEÑO** (§90: `05` decía 6, `10` decía 8, nadie el total). **18 `.astro` + 4 endpoints**. Las **8 fieles a mockup** se detallan abajo; el resto, sin ficha propia porque son de una pantalla: **legales** `terminos` · `privacidad` · `habeas-data` · `legal/politica-tratamiento-datos` · **nav** `aliados` · `invertir` · `journal` · **internas** `404` · `gestion` · `design-system` · **endpoints** `api/solicitud` · `api/catalogo/[operacion].json` · `api/health` · `tiles/[file]`.
- **Páginas** (`src/pages/`): `index.astro` = **HOME COMPLETA 17/17 y FIEL** (§32.15 + §48: hero neumórfico + buscador + maneras + destacadas + arriendo + cerca — su mapa es **ilustrado POR DISEÑO** (§32.18), NO es el de TODO-30 — + valoradas + proyectos + journal + brokers) · **`[operacion].astro`** = SERP dinámico (`/comprar`+`/arrendar`, ADR §25/§55: filtros glass + grid PropertyCard + aside **mapa REAL MapLibre** con fallback esquemático) · **`ficha.astro`** = detalle de inmueble (ADR §26: galería + specs + aside precio/CTA-WhatsApp/sello + amenidades + ficha técnica + ubicación + similares) · **`publicar.astro`** = "Publica tu inmueble" (ADR §27: hero + form de avalúo client-side + 4 pasos + 3 planes) · **`estancias.astro`** = detalle de alojamiento (ADR §28: galería + amenidades + host + widget de reserva funcional por fechas; pago Wompi = Ola 2) · **`turismo.astro`** = landing turismo+inversión (ADR §29: hero + zonas + experiencias + inversión navy + CTA) · `404.astro` = página no encontrada / "en construcción" con Header/Footer (para rutas de nav aún sin construir) · **`gestion.astro`** = panel admin/back-office (ADR §31: sidebar navy + KPIs + tabla pipeline + actividad + demanda; segmentado 3 roles Admin/Aliado/Propietario en JS vanilla sin innerHTML; datos DEMO; noindex vía prop de BaseLayout; SIN Header/Footer) · `design-system.astro` = styleguide dev.
- **Favoritos y acceso** (ADR §89): `src/scripts/favoritos.ts` (módulo sin dependencias) + páginas `src/pages/favoritos.astro` y `src/pages/ingresar.astro`. Los favoritos viven en **localStorage** (`altorra:favoritos:v1`), **NO detrás del login**: el acceso es para SINCRONIZAR, como plantea el mockup. Se guarda una **instantánea leída del DOM** de la card, así `/favoritos` se pinta sin red y sirve con catálogo demo o vivo. Las cards se construyen **clonando el `<template id="tpl-pcard">`** con el PropertyCard real (patrón del SERP; nunca innerHTML). El corazón se cablea desde `BaseLayout` para TODA `.alt-pcard` del sitio; `PropertyCard` etiqueta cada spec con `data-spec` (bed/bath/area) para que el ícono no se separe de su dato. `/ingresar` carga **Firebase Auth con `import()` dinámico solo al enviar**; ⚠️ **crear cuenta NO se abre** hasta publicar la Política de Datos (Ley 1581 art. 9, gate de `42-LEGAL`). Mockup fuente: `design/mockups/ALTORRA Ingresar-Favoritos.dc.html` (8 artboards, importado por el MCP de Claude Design).
- **Leads / captación** (ADR §88): ruta `src/pages/api/solicitud.ts` (POST, `prerender=false`) — ÚNICA escritura pública del portal. Crea en `solicitudes` con el MISMO contrato del legacy (`js/contact-forms.js`) para que `onNewSolicitud` (ya desplegada) la recoja. La escritura de bajo nivel vive en `src/lib/data/firestore-rest.ts` (`encodeValue`+`createDoc`, aditivo: la capa nació read-only) y la config sale de `getPublicFirebaseConfig()` en `client.ts`. El form de `publicar.astro` lleva `method/action`: **funciona sin JavaScript** (POST→303 `?ok=1`). ⚠️ `tipo` del doc = tipo de LEAD; el `<select name="tipo">` es el del INMUEBLE y va en `datosExtra.tipoInmueble`.
- **Dev**: `npm --prefix portal run dev` (`astro dev`, `.claude/launch.json` config `portal`, puerto 4321). Staging LIVE = `altorra-portal.altorrainmobiliaria.workers.dev` (noindex).

## §Blaze — free-tier estricto (NUNCA exceder)
Plan Blaze pero diseñado para **NO costar**. Límites: Firestore 50K lecturas / 20K escrituras / 20K borrados día, 1 GiB ·
Auth 50K MAU · Storage 5GB · Functions 2M inv/mes · RTDB 1GB. **Reglas**: NUNCA `onSnapshot` en colecciones completas
desde público (solo admin); NUNCA queries sin `limit()` (paginar 9-20, default `limit(9)`); caché local antes de Firestore;
preferir `getDoc` (1 lectura) sobre `getDocs` (N); imágenes <200KB; GitHub Actions schedule máx 4h. Escalar más allá → consultar al dueño.

## §Schema Firestore (colecciones en español)
- **`propiedades`** (docID = campo `id`, ej. "101-27"): `titulo`, `slug`, `tipo` (apartamento|casa|lote|oficina|bodega|local),
  `operacion` (comprar|arrendar|dias), `estado` (disponible|reservado|vendido|arrendado), `ciudad`, `barrio`, `direccion`,
  `coords{lat,lng}`, `estrato`, `precio` (COP), `admin_fee`, `habitaciones`, `banos`, `sqm`, `sqm_terreno`, `garajes`, `piso`,
  `ano_construccion`, `amoblado`, `imagen` (thumb), `imagenes[]`, `imagen_og` (1200×630), `features[]`, `descripcion`,
  `featured` (bool), `prioridad` (0-100), `disponible` (bool), `createdAt`, `updatedAt`, `_version` (int), `creadoPor` (UID).
  Subcol `auditLog`: `{accion, campo, antes, despues, usuario, timestamp}`. Mapeo migración data.json(inglés)→Firestore(español): title→titulo, beds→habitaciones, baths→banos, available→disponible, highlightScore→prioridad.
- **`solicitudes`** (leads — **create público `true`**, read solo auth): `nombre`, `telefono`, `email`, `tipo`
  (contacto_propiedad|publicar_propiedad|solicitud_avaluo|solicitud_juridica|otro), `origen`, `estado`
  (pendiente|en_gestion|cerrado), `datosExtra{}`, `createdAt`/`updatedAt`, `emailSent` (idempotencia CF), `requiereCita`.
- **`resenas`**: `{autor, rating 1-5, texto, fecha, fuente (google|directo), activa, orden}`.
- **`usuarios`** (docID = Auth UID; solo super_admin): `{nombre, email, rol (super_admin|editor|viewer), activo, bloqueado, creadoEn, creadoPor}`.
- **`config`**: doc `general` (whatsapp/email/redes/slogan), doc `counters` (totalPropiedades/totalCiudades, editable por editor).
- **`system`**: doc `meta` `{lastModified}` — admin lo actualiza al cambiar propiedades; `cache-manager.js` lo escucha (onSnapshot) para invalidar caché en todas las tabs (receta ⚰️ del sitio viejo → `_legacy/LECCIONES-SITIO-VIEJO.md`).
- **`loginAttempts`** (docID = hash email): `{intentos, bloqueado, ultimoIntento}` — bloqueo tras 5 fallidos.
- **`blog`** → ver §Blog abajo.

## §Reglas (firestore.rules) — resumen
Helpers: `hasProfile()`, `getUserRole()`, `isSuperAdmin()`, `isEditorOrAbove()`, `validVersion()` (req._version == prev+1; create==1).
`propiedades`: read `true`; create/update editorOrAbove+validVersion; delete superAdmin. `solicitudes`: read auth, **create `true`**, update/delete superAdmin.
`resenas`: read `true`, write editor. `usuarios`: own-uid o superAdmin. `config`: read `true`, write superAdmin (editor si `counters`).
`system`: read `true`, write editor. `loginAttempts`: read/create/update `true`, delete superAdmin. Default `/{document=**}`: **deny all**.
Storage: `propiedades/{**}` read `true` write auth; path `propiedades/{id}/{nombre}.webp`, thumb <150KB, OG 1200×630 JPEG q85.

## §Blog (hoja de detalle) — colección `blog`
docID = `slug` (minúsculas/guiones, sin tildes/ñ). Campos: `slug`, `titulo` (55-60 chars), `resumen` (140-160 chars, **= meta description**),
`categoria` (UNA de: Inversión|Rentabilidad|Legal & Fiscal|Análisis|Mercado|Guías), `imagen` (1200×630), `fecha`, `tiempoLectura`,
`publicado` (bool), `url` (opcional → `blog/{slug}.html`), `contenido` (HTML), `autor` (default "Altorra Inmobiliaria"), `_version`, `createdAt`, `updatedAt`.
- **Flujo A (estático SEO-first)**: copiar `blog/_plantilla-post.html` → `blog/{slug}.html`, reemplazar placeholders `{{TITULO}}{{SLUG}}{{FECHA}}{{CATEGORIA}}{{TIEMPO}}{{IMAGEN}}{{RESUMEN}}{{CONTENIDO}}`, añadir a `scripts/upload-blog-posts.mjs` + `sitemap.xml`, JSON-LD `BlogPosting`, commit `feat(blog): new post — {slug}`.
- **Flujo B (dinámico)**: crear doc en `blog` desde admin; `blog.html` + `blog-post.html?slug={slug}` renderizan auto.

## §SEO
URLs sociales compartibles = `/p/{id}.html` (lleva los OG tags, regenerado por GitHub Actions `og-publish.yml`); detalle interno = `detalle-propiedad.html?id={id}`. 43 páginas con `BreadcrumbList` JSON-LD. 13 landings de sector con props dinámicas (`sector-properties.js`).
