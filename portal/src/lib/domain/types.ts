// Tipos del dominio — SKELETON (capa de dominio).
//
// Los esquemas v1 (propiedades, solicitudes, disponibilidad, config + GESTIÓN:
// contratos, pagos, novedades, expedientes) se definen en Ola 0 ítem 7, a partir del
// destilado R0 y los .xlsx FTI-01 del dueño (2ª pasada pendiente del crítico R4).
//
// Placeholder de módulo para que la capa exista desde el scaffold.
export {};
